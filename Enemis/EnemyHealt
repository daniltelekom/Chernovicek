//Добавить сверху 
public bool isBoss = false;
//А в любом эффекте:
if (enemy.isBoss) return;
//Добавить
public static System.Action OnAnyEnemyKilled;

void Die()
{
    OnAnyEnemyKilled?.Invoke();
    // остальное
}

В EnemyHealth.Die() (или где у тебя смерть):

Добавь поля в EnemyHealth:

public GameObject lootPrefab;
public ItemData debugDropItem; // для теста

В Die() добавь:

if (lootPrefab != null && debugDropItem != null)
{
    var loot = Instantiate(lootPrefab, transform.position, Quaternion.identity);
    var pick = loot.GetComponent<LootPickup>();
    if (pick != null) pick.item = debugDropItem;
}
